#include<iostream>
#include<map>
using namespace std;

int main()
{
	map<long int , bool> map1;
	long int a[20];
	int n;
	cin >> n;
	for (int i=0;i<n;i++)
	{
		cin>a[i];
		map1.insert(a[i],true);
	}
	
	int i,j;
	
	for(i=0;i<n;i++)
	{
		for(j=i+1;j<n;j++)
		{
			if(a
return 0;
}

	
	
